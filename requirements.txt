# === 普通依赖：pip install -r requirements.txt ===
anthropic>=0.112.0
captum>=0.8.0
einops>=0.8.0
joblib>=1.5.0
linear-attention-transformer>=0.19.0
lmdb>=1.7.0
matplotlib>=3.8.0
matplotlib-venn>=1.1.0
mne>=1.6.0
numpy>=1.24.0
openai>=2.44.0
PyYAML>=6.0
scikit-learn>=1.3.0
scipy>=1.10.0
shap>=0.45.0
timm>=1.0.0
transformers>=4.40.0
typing-extensions>=4.10.0

# === 需要单独安装（不要直接 pip install -r 一把梭）===
# 1) torch：按 CUDA 版本从 https://pytorch.org/get-started/locally/ 安装
#    示例（CUDA 12.1）：
#    pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cu121
#
# 2) mamba 相关（需要 GPU + CUDA + 编译器，Windows 较难装；仅运行 EEGMamba 模型时需要）：
#    pip install packaging ninja
#    pip install causal-conv1d>=1.4.0
#    pip install mamba-ssm>=2.0 --no-build-isolation
